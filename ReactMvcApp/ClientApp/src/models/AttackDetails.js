export default class AttackDetails {
  AttackDetails(attackDescription, date, userId, attackType) {
    this.attackDescription = attackDescription;
    this.date = date;
    this.userId = userId;
    this.attackType = attackType;
  }
}
